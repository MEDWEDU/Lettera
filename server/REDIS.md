# Redis Integration Documentation

Этот документ описывает интеграцию Redis для хранения сессий, кэширования и временных данных в Lettera сервере.

## Обзор

Redis используется для:
- 🔐 **Email-кодов** - временное хранение кодов подтверждения email (TTL: 10 минут)
- 🔑 **Refresh-токенов** - хранение refresh токенов для авторизации (TTL: 7 дней)  
- 👤 **Статусов пользователей** - онлайн статусы с heartbeat (TTL: 5 минут)

## Установка и Настройка

### 1. Зависимости

Redis интеграция использует `ioredis` версии 5.x+:
```bash
npm install ioredis
```

### 2. Переменные окружения

Добавьте в `.env` файл:

```bash
# Redis Configuration for Sessions and Caching
REDIS_URL=redis://localhost:6379

# Redis Connection Settings
REDIS_MAX_RETRIES=3
REDIS_RETRY_DELAY=1000
REDIS_CONNECT_TIMEOUT=5000

# TTL Settings (в секундах)
REDIS_EMAIL_CODE_TTL=600
REDIS_REFRESH_TOKEN_TTL=604800
REDIS_USER_STATUS_TTL=300
```

### 3. Конфигурация Production

Для production окружения используйте:
```bash
REDIS_URL=redis://username:password@localhost:6379
```

## Архитектура

### Структура файлов

```
src/database/
├── config/
│   ├── redis/
│   │   ├── redisConfig.ts       # Конфигурация Redis
│   │   └── redisConnection.ts   # Подключение к Redis
│   └── connection.ts            # MongoDB (существующий)
├── redis/
│   ├── redisUtils.ts           # Utility функции
│   └── index.ts                # Redis модуль
└── index.ts                    # Главный экспорт
```

### Ключи Redis

Структура ключей для организации данных:

- `email:code:{email}` - Email коды подтверждения
- `user:refresh_token:{userId}` - Refresh токены (SET)
- `user:status:{userId}` - Статусы пользователей

## API Reference

### Email Code Functions

#### setEmailCode(email, code, ttl?)
Сохранить код подтверждения email.

```typescript
import { setEmailCode } from './database';

await setEmailCode('user@example.com', '123456', 600); // TTL 10 минут
```

**Параметры:**
- `email` - Email адрес
- `code` - Код подтверждения
- `ttl` - Время жизни в секундах (опционально, по умолчанию 600)

**Возвращает:** `Promise<boolean>` - Успех операции

#### getEmailCode(email)
Получить код подтверждения email.

```typescript
import { getEmailCode } from './database';

const code = await getEmailCode('user@example.com');
// Возвращает код или null если не найден
```

**Параметры:**
- `email` - Email адрес

**Возвращает:** `Promise<string | null>` - Код подтверждения или null

#### deleteEmailCode(email)
Удалить код подтверждения email после верификации.

```typescript
import { deleteEmailCode } from './database';

await deleteEmailCode('user@example.com');
```

**Параметры:**
- `email` - Email адрес

**Возвращает:** `Promise<boolean>` - Успех операции

### Refresh Token Functions

#### setRefreshToken(userId, token, ttl?)
Сохранить refresh токен.

```typescript
import { setRefreshToken } from './database';

await setRefreshToken('user123', 'refresh_token_abc', 604800); // TTL 7 дней
```

**Параметры:**
- `userId` - ID пользователя
- `token` - Refresh токен
- `ttl` - Время жизни в секундах (опционально, по умолчанию 604800)

**Возвращает:** `Promise<boolean>` - Успех операции

#### getRefreshToken(userId)
Получить refresh токен пользователя.

```typescript
import { getRefreshToken } from './database';

const token = await getRefreshToken('user123');
// Возвращает токен или null если не найден
```

**Параметры:**
- `userId` - ID пользователя

**Возвращает:** `Promise<string | null>` - Refresh токен или null

#### invalidateAllRefreshTokens(userId)
Инвалидировать все refresh токены пользователя (при смене пароля).

```typescript
import { invalidateAllRefreshTokens } from './database';

await invalidateAllRefreshTokens('user123');
```

**Параметры:**
- `userId` - ID пользователя

**Возвращает:** `Promise<boolean>` - Успех операции

### User Status Functions

#### setUserStatus(userId, status, ttl?)
Установить статус пользователя с TTL.

```typescript
import { setUserStatus } from './database';

await setUserStatus('user123', 'online', 300); // TTL 5 минут
```

**Параметры:**
- `userId` - ID пользователя
- `status` - Статус ('online', 'offline', 'away', 'busy')
- `ttl` - Время жизни в секундах (опционально, по умолчанию 300)

**Возвращает:** `Promise<boolean>` - Успех операции

#### getUserStatus(userId)
Получить статус пользователя.

```typescript
import { getUserStatus } from './database';

const status = await getUserStatus('user123');
// Возвращает статус или null если пользователь офлайн
```

**Параметры:**
- `userId` - ID пользователя

**Возвращает:** `Promise<string | null>` - Статус или null

### Utility Functions

#### isRedisHealthy()
Проверить состояние Redis подключения.

```typescript
import { isRedisHealthy } from './database';

const isHealthy = await isRedisHealthy();
```

**Возвращает:** `Promise<boolean>` - Состояние подключения

#### getRedisConnectionState()
Получить детальную информацию о подключении.

```typescript
import { getRedisConnectionState } from './database';

const state = getRedisConnectionState();
console.log(state);
// {
//   status: 'ready',
//   connected: true,
//   host: 'localhost',
//   port: 6379
// }
```

**Возвращает:** Объект с информацией о подключении

## Fallback Strategy

Если Redis недоступен, система автоматически переключается на in-memory storage:

### Преимущества:
- ✅ **Бесшовное переключение** - приложение продолжает работать
- ✅ **Никаких изменений в коде** - API остается тем же
- ✅ **Логирование** - все переключения логируются

### Ограничения Fallback:
- ⚠️ **Не персистентный** - данные теряются при перезапуске
- ⚠️ **Не масштабируемый** - только для одного процесса
- ⚠️ **TTL не гарантирован** - очистка происходит только при обращении

### Рекомендации:
- Используйте Redis в production
- Fallback предназначен только для development и graceful degradation

## Health Checks

### Основной health check
```bash
GET /api/health
```

Возвращает информацию о всех подключениях (MongoDB + Redis).

### Redis health check
```bash
GET /api/health/redis
```

Возвращает детальную информацию о Redis подключении:
```json
{
  "redis": {
    "connected": true,
    "status": "ready",
    "host": "localhost",
    "port": 6379,
    "timestamp": "2024-01-01T00:00:00.000Z",
    "fallback": false
  }
}
```

## Graceful Shutdown

Система автоматически обрабатывает graceful shutdown:

1. **SIGTERM** - Корректное закрытие Redis подключения
2. **SIGINT** (Ctrl+C) - Корректное закрытие Redis подключения
3. **SIGUSR2** (Nodemon) - Закрытие перед рестартом

### Настройка вручную:
```typescript
import { setupRedisGracefulShutdown } from './database';

setupRedisGracefulShutdown();
```

## Тестирование

### Запуск тестов Redis:
```bash
npm run test-redis
```

Тесты проверяют:
- ✅ Подключение к Redis
- ✅ Все функции email кодов
- ✅ Все функции refresh токенов  
- ✅ Все функции статусов пользователей
- ✅ Обработку ошибок
- ✅ Fallback функциональность

### Комплексное тестирование:
```bash
npm run test-all
```

Тестирует MongoDB + Redis вместе.

## Production Considerations

### Производительность:
- **Connection Pool** - ioredis управляет пулом соединений автоматически
- **TTL** - автоматическая очистка истекших данных
- **Pipeline** - используйте MGET/MSET для батчевых операций

### Мониторинг:
- Логи подключения/отключения в консоли
- Health check endpoints для внешнего мониторинга
- Детальное логирование операций

### Безопасность:
- Все ключи организованы с префиксами
- TTL предотвращает накопление старых данных
- Graceful shutdown предотвращает потерю данных

## Примеры использования

### Email Verification Flow
```typescript
// 1. Отправка кода
await setEmailCode('user@example.com', '123456');

// 2. Проверка кода
const storedCode = await getEmailCode('user@example.com');
if (storedCode === '123456') {
  // Код верный, подтверждаем email
  await deleteEmailCode('user@example.com');
}
```

### Authentication Flow
```typescript
// 1. Сохранение refresh токена при логине
await setRefreshToken(userId, refreshToken);

// 2. Обновление токена
const storedToken = await getRefreshToken(userId);
if (storedToken === presentedToken) {
  // Токен валиден, выдаем новый access token
}

// 3. Инвалидация при смене пароля
await invalidateAllRefreshTokens(userId);
```

### Online Status Flow
```typescript
// 1. Установка статуса при подключении
await setUserStatus(userId, 'online');

// 2. Обновление статуса
await setUserStatus(userId, 'away');

// 3. Получение статуса для отображения
const status = await getUserStatus(userId);
// 'online' | 'away' | 'busy' | 'offline' | null
```

## Troubleshooting

### Частые проблемы:

1. **Redis недоступен**
   - Проверьте подключение: `redis-cli ping`
   - Убедитесь что REDIS_URL правильный
   - Проверьте firewall и права доступа

2. **Fallback активируется**
   - Нормально для development
   - Логируются все переключения
   - В production должен быть Redis

3. **TTL не работает**
   - Проверьте версию Redis (2.0+ поддерживает EXPIRE)
   - Убедитесь что ключи устанавливаются с TTL

4. **Потеря данных при перезапуске**
   - Для персистентности используйте Redis persistence
   - Или используйте MongoDB для критичных данных

### Отладка:

```typescript
// Включить подробное логирование
console.log('Redis State:', getRedisConnectionState());
console.log('Redis Health:', await isRedisHealthy());
```

## Миграция и обновления

### Добавление новых функций:
1. Добавьте в `redisUtils.ts`
2. Обновите экспорты в `index.ts`
3. Добавьте тесты в `test-redis.ts`
4. Обновите документацию

### Обновление конфигурации:
1. Измените `redisConfig.ts`
2. Обновите `.env.example`
3. Протестируйте изменения

---

**Версия:** 1.0.0  
**Последнее обновление:** 2024  
**Автор:** Lettera Development Team